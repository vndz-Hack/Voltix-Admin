local replicate_storage = game:service"ReplicatedStorage";
local players = game:service"Players";

local local_player = players.LocalPlayer;
local events = replicate_storage.Events;

events.LoadCharacter(3);
